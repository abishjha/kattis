#include<iostream>

int main() {
     int numOfCases;
     std::cin >> numOfCases;
     int sum, diff, a, b;

     for (int i = 0; i < numOfCases; i++) {
          std::cin >> sum >> diff;
          if (sum > diff) {

          }
     }

     system("pause");
}